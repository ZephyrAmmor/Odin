{
  "name": "todo",
  "version": "1.0.0",
  "description": "* We need to create a Web App that allows users to    - Create `boards`, `projects` in `boards` and `tasks` in `projects`.   - Add `projects` to `boards` and `tasks` to `projects`   - Edit `boards`, `projects`, `tasks` or delete them.   - Store there data in the `localStorage` of browser.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@eslint/js": "^9.34.0",
    "css-loader": "^7.1.2",
    "eslint": "^9.34.0",
    "globals": "^16.3.0",
    "html-webpack-plugin": "^5.6.4",
    "style-loader": "^4.0.0",
    "webpack": "^5.101.3",
    "webpack-cli": "^6.0.1",
    "webpack-dev-server": "^5.2.2"
  }
}
